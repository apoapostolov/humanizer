interface:
  display_name: "Humanizer"
  short_description: "Humanize text while preserving voice"
  default_prompt: "Use $humanizer to rewrite or audit this text naturally while preserving its meaning and voice."
