I fixed the mount bug yesterday. qB was still pointed at the old path after the
reboot, so nothing imported until I flipped the category and forced a recheck.
Prowlarr indexes were fine; the handoff folder on E: was the problem.
